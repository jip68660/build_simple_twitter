import React from 'react';

const StatusContent = (props) => {
    return(
        <div className ="context">
            <p>{ props.text }</p>
        </div>
    );
};

export default StatusContent;
